<Nav />
<div class="container">
  <div class="row row-cols-3">
  {{!-- {{#each @model as |kanji|}}
  <h1>{{kanji.kanji}}</h1>
  {{/each}} --}}
  {{#each @model as |kanji|}}
  <Flashcard @kanji={{kanji}}/>
  {{/each}}
  </div>
</div>
